<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:beans="http://www.springframework.org/schema/beans"
	xmlns:context="http://www.springframework.org/schema/context"
	xmlns:task="http://www.springframework.org/schema/task"
	xmlns:mvc="http://www.springframework.org/schema/mvc"
	xsi:schemaLocation="http://www.springframework.org/schema/task http://www.springframework.org/schema/task/spring-task-4.3.xsd
		http://www.springframework.org/schema/mvc https://www.springframework.org/schema/mvc/spring-mvc.xsd
		http://www.springframework.org/schema/beans https://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/context https://www.springframework.org/schema/context/spring-context.xsd">

	<!-- DispatcherServlet Context: defines this servlet's request-processing infrastructure -->
	
	<!-- Enables the Spring MVC @Controller programming model -->
	<mvc:annotation-driven/>
	<!-- Handles HTTP GET requests for /resources/** by efficiently serving up static resources in the ${webappRoot}/resources directory -->
	<mvc:resources mapping="/resources/**" location="/resources/" />

	<!-- Resolves views selected for rendering by @Controllers to .jsp resources in the /WEB-INF/views directory -->
	<beans:bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
		<beans:property name="prefix" value="/WEB-INF/views/" />
		<beans:property name="suffix" value=".jsp" />
	</beans:bean>
	
	<!-- 회원 -->
	<context:component-scan base-package="com.ryan.controller" />
	<context:component-scan base-package="com.ryan.exception"/>
	
	<!-- 관리자 -->
	<context:component-scan base-package="com.admin.controller"/>
	
<!-- 	   timer 설정   
 	<task:scheduler id="gsScheduler" pool-size="10" />
 	<task:executor id="gsTaskExecutor" pool-size="10" />
 	<task:annotation-driven executor="gsTaskExecutor" scheduler="gsScheduler" />  -->
	
	<!-- interceptor 설정 -->
	<beans:bean id="cookieInterceptor" class="com.ryan.interceptor.CookieInterceptor"/>
	<beans:bean id="loginSessionChkInterceptor" class="com.ryan.interceptor.LoginSessionChkInterceptor"/>
	
	<!-- 자동로그인 쿠키 확인 -->
	<mvc:interceptors>
		<mvc:interceptor>
			<mvc:mapping path="/**/*"/>
			<mvc:mapping path="/**"/>
			<mvc:exclude-mapping path="/member/signin"/>
			<mvc:exclude-mapping path="/member/logout"/>
			<mvc:exclude-mapping path="/member/signUp"/>
			<beans:ref bean="cookieInterceptor"/>
		</mvc:interceptor>
	</mvc:interceptors>
	
	<mvc:interceptors>
		<mvc:interceptor>

			<mvc:mapping path="/cart/*"/> <!-- 로그인 유효성 검사할 페이지들 입력. -->
			<mvc:mapping path="/book/insertlike"/>

			<beans:ref bean="loginSessionChkInterceptor"/>
		</mvc:interceptor>
	</mvc:interceptors>
	
</beans:beans>
